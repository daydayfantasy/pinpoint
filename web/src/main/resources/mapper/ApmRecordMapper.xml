<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">


<mapper namespace="com.navercorp.pinpoint.web.dao.ApmRecordDao">


    <insert id="insertApmRecord" parameterType="ApmRecord">
        INSERT INTO apm_record (applicationId, serviceType, inTime, smsMessage, checkName)
        VALUES (#{applicationId}, #{serviceType}, #{inTime}, #{smsMessage}, #{checkName})
    </insert>
       

</mapper>
